const STATIONS=["本通","県庁前","城北","新白島","白島","牛田","不動院前","祇園新橋北","西原","中筋","古市","大町","毘沙門台","安東","上安","高取","長楽寺","伴","大原","伴中央","大塚","広域公園前"];
const KILOPOSTS={"本通":0.0,"県庁前":0.3,"城北":1.4,"新白島":1.7,"白島":2.1,"牛田":2.9,"不動院前":4.0,"祇園新橋北":5.0,"西原":6.0,"中筋":7.0,"古市":7.8,"大町":8.4,"毘沙門台":9.6,"安東":10.6,"上安":11.4,"高取":12.0,"長楽寺":12.7,"伴":13.9,"大原":14.9,"伴中央":16.0,"大塚":17.6,"広域公園前":18.4};
const FARE_BRACKETS=[[2,220,110],[4,260,130],[6,300,150],[9,350,180],[12,400,200],[15,430,220],[18,460,230],[19,490,250]];
const DIRECTION_HONDORI="本通方面",DIRECTION_KOIKI="広域公園前方面",SERVICE_WEEKDAY="weekday",SERVICE_HOLIDAY="holiday";let trips=[],holidays={},deferredInstallPrompt=null;const $=id=>document.getElementById(id);
function stationIndex(s){return STATIONS.indexOf(s)}function getDirection(f,t){if(f===t)return null;return stationIndex(t)<stationIndex(f)?DIRECTION_HONDORI:DIRECTION_KOIKI}function distanceKm(f,t){return Math.round(Math.abs(KILOPOSTS[t]-KILOPOSTS[f])*10)/10}function fareFor(f,t){if(f===t)return{adult:0,child:0,km:0};const km=distanceKm(f,t),b=FARE_BRACKETS.find(x=>km<=x[0]);return{adult:b[1],child:b[2],km}}function parseHHMM(v){const s=String(v??"").trim().replace("：",":");if(!s)return null;let h,m;if(s.includes(":")){const p=s.split(":");h=Number(p[0]);m=Number(p[1])}else{const d=s.replace(/\D/g,"");if(d.length<=2)return null;h=Number(d.slice(0,-2));m=Number(d.slice(-2))}if(!Number.isInteger(h)||!Number.isInteger(m)||m<0||m>59||h<0||h>29)return null;return h*60+m}function formatHHMM(min){return `${String(Math.floor(min/60)).padStart(2,"0")}:${String(min%60).padStart(2,"0")}`}function todayKey(d){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`}function serviceType(d){const day=d.getDay();return (day===0||day===6||holidays[todayKey(d)])?SERVICE_HOLIDAY:SERVICE_WEEKDAY}function serviceLabel(d){if(serviceType(d)===SERVICE_WEEKDAY)return "平日ダイヤ";const name=holidays[todayKey(d)];return name?`土曜・休日ダイヤ（${name}）`:"土曜・休日ダイヤ"}function loadSettings(){try{return JSON.parse(localStorage.getItem("astramSettings")||"{}")}catch{return{}}}function saveSettings(){const s=loadSettings();s.from=$("fromStation").value;s.to=$("toStation").value;localStorage.setItem("astramSettings",JSON.stringify(s))}function setupStations(){for(const st of STATIONS){$("fromStation").append(new Option(st,st));$("toStation").append(new Option(st,st))}const s=loadSettings();$("fromStation").value=s.from||"不動院前";$("toStation").value=s.to||"本通"}async function loadHolidays(){try{const r=await fetch("https://holidays-jp.github.io/api/v1/date.json",{cache:"force-cache"});holidays=await r.json();localStorage.setItem("jpHolidays",JSON.stringify(holidays))}catch{try{holidays=JSON.parse(localStorage.getItem("jpHolidays")||"{}")}catch{holidays={}}}}function parseCSV(text){const rows=[];let row=[],cell="",q=false;for(let i=0;i<text.length;i++){const ch=text[i],nx=text[i+1];if(ch==='"'&&q&&nx==='"'){cell+='"';i++}else if(ch==='"'){q=!q}else if(ch===','&&!q){row.push(cell);cell=""}else if((ch==='\n'||ch==='\r')&&!q){if(ch==='\r'&&nx==='\n')i++;row.push(cell);cell="";if(row.some(v=>v.trim()!==""))rows.push(row);row=[]}else{cell+=ch}}row.push(cell);if(row.some(v=>v.trim()!==""))rows.push(row);return rows}async function loadTrips(){try{const r=await fetch("./astram_trips.csv",{cache:"no-store"});const text=await r.text();const rows=parseCSV(text);if(rows.length<=1){trips=[];return}const headers=rows[0].map(h=>h.trim().replace(/^\uFEFF/,""));trips=rows.slice(1).map((cols,idx)=>{const o={};headers.forEach((h,i)=>o[h]=cols[i]??"");const times={};for(const st of STATIONS){const t=parseHHMM(o[st]);if(t!==null)times[st]=t}return{tripId:o.trip_id||`row${idx+2}`,service:o.service,direction:o.direction,destination:o.destination,times}}).filter(t=>[SERVICE_WEEKDAY,SERVICE_HOLIDAY].includes(t.service)&&[DIRECTION_HONDORI,DIRECTION_KOIKI].includes(t.direction))}catch(e){console.warn(e);trips=[]}}function findNextTrains(f,t,now,count=3){const dir=getDirection(f,t),svc=serviceType(now),nowMin=now.getHours()*60+now.getMinutes();return trips.filter(x=>x.service===svc&&x.direction===dir&&x.times[f]!=null&&x.times[t]!=null).map(x=>({trip:x,depart:x.times[f],arrive:x.times[t]})).filter(x=>x.arrive>=x.depart&&x.depart>=nowMin).sort((a,b)=>a.depart-b.depart).slice(0,count)}function function getSearchDateTime() {
  const now = new Date();
  const timeInput = document.getElementById("searchTime");

  if (!timeInput || !timeInput.value) {
    return now;
  }

  const [hour, minute] = timeInput.value.split(":").map(Number);

  const searchDate = new Date();
  searchDate.setHours(hour);
  searchDate.setMinutes(minute);
  searchDate.setSeconds(0);
  searchDate.setMilliseconds(0);

  return searchDate;
}
render(){const now=getSearchDateTime();$("clock").textContent=now.toLocaleString("ja-JP",{hour12:false});const f=$("fromStation").value,t=$("toStation").value;saveSettings();const fare=fareFor(f,t);$("fareText").textContent=`運賃: 大人 ${fare.adult}円 / 小児・割引 ${fare.child}円`;$("serviceBadge").textContent=serviceLabel(now);$("dataStatus").textContent=`収録列車: ${trips.length}本（平日/休日・上り/下り 完全収録）`;if(f===t){$("directionText").textContent="同じ駅";$("mainTrain").textContent="出発駅と到着駅が同じです";$("subInfo").textContent="";$("nextList").innerHTML="";return}const dir=getDirection(f,t);$("directionText").textContent=dir;const next=findNextTrains(f,t,now,3);if(trips.length===0){$("mainTrain").textContent="時刻表CSVにデータがありません";$("subInfo").textContent="";$("nextList").innerHTML="";return}if(next.length===0){$("mainTrain").textContent="この条件の列車が見つかりません";$("subInfo").textContent="終電後、またはCSVにこの区間のデータがない可能性があります。";$("nextList").innerHTML="";return}const first=next[0],until=first.depart-(now.getHours()*60+now.getMinutes());$("mainTrain").textContent=`${formatHHMM(first.depart)} 発 → ${formatHHMM(first.arrive)} 着`;$("subInfo").textContent=`あと${until}分 / 所要${first.arrive-first.depart}分 / 行先 ${first.trip.destination}`;$("nextList").innerHTML=next.map((x,i)=>`<div class="next-item"><strong>${i===0?"次":`${i+1}本目`}: ${formatHHMM(x.depart)} → ${formatHHMM(x.arrive)}</strong><br><span class="muted">所要${x.arrive-x.depart}分 / ${x.trip.destination} </span></div>`).join("")}function renderFavorites(){const s=loadSettings(),favs=s.favorites||[];$("favorites").innerHTML=favs.length?favs.map((f,i)=>`<button class="favorite-chip" data-index="${i}">${f.from} → ${f.to}</button>`).join(""):'<p class="muted">まだ登録されていません。</p>';document.querySelectorAll(".favorite-chip").forEach(btn=>btn.addEventListener("click",()=>{const f=favs[Number(btn.dataset.index)];$("fromStation").value=f.from;$("toStation").value=f.to;render()}))}function addFavorite(){const s=loadSettings();s.favorites=s.favorites||[];const fav={from:$("fromStation").value,to:$("toStation").value};if(!s.favorites.some(x=>x.from===fav.from&&x.to===fav.to))s.favorites.push(fav);localStorage.setItem("astramSettings",JSON.stringify(s));renderFavorites()}async function init(){setupStations();await loadHolidays();await loadTrips();renderFavorites();render();$("fromStation").addEventListener("change",render);$("toStation").addEventListener("change",render);$("swapButton").addEventListener("click",()=>{const a=$("fromStation").value;$("fromStation").value=$("toStation").value;$("toStation").value=a;render()});$("favoriteButton").addEventListener("click",addFavorite);$("reloadButton").addEventListener("click",async()=>{await loadTrips();render()});setInterval(render,30000)}window.addEventListener("beforeinstallprompt",e=>{e.preventDefault();deferredInstallPrompt=e;$("installButton").classList.remove("hidden")});$("installButton").addEventListener("click",async()=>{if(!deferredInstallPrompt)return;deferredInstallPrompt.prompt();await deferredInstallPrompt.userChoice;deferredInstallPrompt=null;$("installButton").classList.add("hidden")});if("serviceWorker" in navigator)window.addEventListener("load",()=>navigator.serviceWorker.register("./service-worker.js"));init();
